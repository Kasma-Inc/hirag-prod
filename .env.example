# Generated by hirag_prod.configs.ConfigManager.to_dotenv_example(file_path=.env.example, only_no_default=True).

##### S3Config
# Required str. 
OFNIL_STAGE_REGION=
# Required str. 
OFNIL_STAGE_ACCESS_KEY_ID=
# Required SecretStr. 
OFNIL_STAGE_SECRET_ACCESS_KEY=
# Required str. 
OFNIL_STAGE_BUCKET_NAME=
# Optional str. Endpoint URL. Put "null" to let the client infer the endpoint (for AWS only).
OFNIL_STAGE_ENDPOINT=

##### DotsOCRConfig
# Required str. The base URL of the Dots OCR API.
DOTS_OCR_BASE_URL=
# Required SecretStr. The API key for the Dots OCR API.
DOTS_OCR_API_KEY=

##### EmbeddingConfig
# Required str. The base URL of the embedding service. If service type is openai, it will be overwritten by openai_base_url.
EMBEDDING_BASE_URL=
# Required SecretStr. The API key of the embedding service. If service type is openai, it will be overwritten by openai_api_key.
EMBEDDING_API_KEY=

##### LLMConfig
# Required str. The base URL of the LLM service. If service type is openai, it will be overwritten by openai_base_url.
LLM_BASE_URL=
# Required SecretStr. The API key of the LLM service. If service type is openai, it will be overwritten by openai_api_key.
LLM_API_KEY=

##### PostgresDBConfig
# Required PostgresDsn. Postgres URL
# Examples: postgres://user:password@postgresdb:5432/database
POSTGRES_URL=

##### RerankConfig
# Required str. The base URL of the reranker service.
RERANKER_BASE_URL=
# Required SecretStr. The API key of the reranker service.
RERANKER_API_KEY=

##### TranslatorConfig
# Required str. The base URL of the translator service. If service type is openai, it will be overwritten by openai_base_url.
TRANSLATOR_BASE_URL=
# Required SecretStr. The API key of the translator service. If service type is openai, it will be overwritten by openai_api_key.
TRANSLATOR_API_KEY=
