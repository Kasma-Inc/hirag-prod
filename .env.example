# Generated by hirag_prod.configs.ConfigManager.to_dotenv_example(file_path=.env.example, only_no_default=False).

##### Configurations
# Required bool. 
ENABLE_TOKEN_COUNT=False
# Required str. 
VOYAGE_API_KEY=

##### S3Config
# Required str. 
OFNIL_STAGE_REGION=
# Required str. 
OFNIL_STAGE_ACCESS_KEY_ID=
# Required SecretStr. 
OFNIL_STAGE_SECRET_ACCESS_KEY=
# Required str. 
OFNIL_STAGE_BUCKET_NAME=
# Optional str. 
OFNIL_STAGE_ENDPOINT=

##### DotsOCRConfig
# Required str. The base URL of the Dots OCR API.
DOTS_OCR_BASE_URL=
# Required SecretStr. The API key for the Dots OCR API.
DOTS_OCR_API_KEY=
# Required str. The DotsOCR model name.
DOTS_OCR_MODEL_NAME=DotsOCR
# Required str. The entry point for the Dots OCR API.
DOTS_OCR_ENTRY_POINT=/parse/file
# Required int. The timeout in seconds for the Dots OCR requests.
DOTS_OCR_TIMEOUT=300
# Required int. The polling interval in seconds for async jobs
DOTS_OCR_POLLING_INTERVAL=5
# Required int. The number of retries for polling requests.
DOTS_OCR_POLLING_RETRIES=3
# Required int. The max number of requests per unit time.
DOTS_OCR_RATE_LIMIT=60
# Choices: [second, minute, hour]. The time unit for the rate limit.
DOTS_OCR_RATE_LIMIT_TIME_UNIT=minute
# Required float. The min interval in seconds between requests to Dots OCR.
DOTS_OCR_RATE_LIMIT_MIN_INTERVAL_SECONDS=0.1

##### EmbeddingConfig
# Choices: [openai, local]. The type of the embedding service.
EMBEDDING_SERVICE_TYPE=local
# Required str. The base URL of the embedding service. If service type is openai, it will be overwritten by openai_base_url.
EMBEDDING_BASE_URL=
# Required SecretStr. The API key of the embedding service. If service type is openai, it will be overwritten by openai_api_key.
EMBEDDING_API_KEY=
# Required str. The entry point of the embedding service.
EMBEDDING_ENTRY_POINT=/v1/embeddings
# Optional str. The name of the local embedding model. Required when service type is local.
EMBEDDING_MODEL_NAME=Qwen3-Embedding-8B
# Optional str. The path of the local embedding model. Required when service type is local.
EMBEDDING_MODEL_PATH=Qwen3-Embedding-8B
# Required int. The default batch size for local embedding service. Useful only when service type is local.
EMBEDDING_DEFAULT_BATCH_SIZE=1000
# Required int. The max number of requests per unit time.
EMBEDDING_RATE_LIMIT=6000
# Choices: [second, minute, hour]. The time unit for the rate limit.
EMBEDDING_RATE_LIMIT_TIME_UNIT=minute
# Required float. The min interval in seconds between requests to the embedding service.
EMBEDDING_RATE_LIMIT_MIN_INTERVAL_SECONDS=0.1
# Optional str. The base URL of the OpenAI embedding service. Useful only when service type is openai.
EMBEDDING_OPENAI_BASE_URL=
# Optional SecretStr. The API key of the OpenAI embedding service. Useful only when service type is openai.
EMBEDDING_OPENAI_API_KEY=

##### HiRAGConfig
# Choices: [en, cn-s, cn-t]. The language used for prompts.
HI_RAG_LANGUAGE=en
# Choices: [pgvector]. 
HI_RAG_VDB_TYPE=pgvector
# Required int. 
HI_RAG_CHUNK_SIZE=1200
# Required int. 
HI_RAG_CHUNK_OVERLAP=200
# Required bool. Whether to construct graph for indexing. Constructing graph improves the retrieval quality at the cost of token usage.
HI_RAG_CONSTRUCT_GRAPH=False
# Required int. 
HI_RAG_EMBEDDING_BATCH_SIZE=1000
# Required int. 
HI_RAG_ENTITY_UPSERT_CONCURRENCY=32
# Required int. 
HI_RAG_RELATION_UPSERT_CONCURRENCY=32
# Required int. 
HI_RAG_MAX_RETRIES=3
# Required float. 
HI_RAG_RETRY_DELAY=1.0
# Required float. 
HI_RAG_SIMILARITY_THRESHOLD=0.5
# Required float. 
HI_RAG_SIMILARITY_MAX_DIFFERENCE=0.15
# Required int. 
HI_RAG_MAX_REFERENCES=3
# Required int. 
HI_RAG_MAX_CHUNK_IDS_PER_QUERY=10
# Required int. 
HI_RAG_DEFAULT_QUERY_TOP_K=10
# Required int. 
HI_RAG_DEFAULT_QUERY_TOP_N=5
# Required int. 
HI_RAG_DEFAULT_LINK_TOP_K=30
# Required float. 
HI_RAG_DEFAULT_PASSAGE_NODE_WEIGHT=0.6
# Required float. 
HI_RAG_DEFAULT_PAGERANK_DAMPING=0.5
# Required int. 
HI_RAG_CLUSTERING_N_CLUSTERS=3
# Required float. 
HI_RAG_CLUSTERING_DISTANCE_THRESHOLD=0.5
# Choices: [ward, complete, average, single]. 
HI_RAG_CLUSTERING_LINKAGE_METHOD=ward
# Choices: [fixed, distance]. 
HI_RAG_CLUSTERING_N_TYPE=fixed
# Required float. 
HI_RAG_DEFAULT_DISTANCE_THRESHOLD=0.8

##### LLMConfig
# Choices: [openai, local]. The type of the LLM service.
LLM_SERVICE_TYPE=openai
# Required str. The base URL of the LLM service. If service type is openai, it will be overwritten by openai_base_url.
LLM_BASE_URL=
# Required SecretStr. The API key of the LLM service. If service type is openai, it will be overwritten by openai_api_key.
LLM_API_KEY=
# Required str. The entry point of the LLM service.
LLM_ENTRY_POINT=/v1/chat/completions
# Required str. The LLM model name.
LLM_MODEL_NAME=gpt-4o-mini
# Required int. The maximum number of tokens that can be generated in the chat completion.
LLM_MAX_TOKENS=16000
# Required float. The timeout in seconds for the LLM requests.
LLM_TIMEOUT=30.0
# Required int. The max number of requests per unit time.
LLM_RATE_LIMIT=60
# Choices: [second, minute, hour]. The time unit for the rate limit.
LLM_RATE_LIMIT_TIME_UNIT=minute
# Required float. The min interval in seconds between requests to the embedding service.
LLM_RATE_LIMIT_MIN_INTERVAL_SECONDS=0.1
# Optional str. The base URL of the OpenAI embedding service. Useful only when service type is openai.
LLM_OPENAI_BASE_URL=
# Optional SecretStr. The API key of the OpenAI embedding service. Useful only when service type is openai.
LLM_OPENAI_API_KEY=

##### PostgresDBConfig
# Required PostgresDsn. Postgres URL
# Examples: postgres://user:password@postgresdb:5432/database
POSTGRES_URL=
# Required str. Postgres table name for storing knowledge base catalog
POSTGRES_TABLE_NAME=KnowledgeBaseCatalog
# Required str. Postgres schema
POSTGRES_SCHEMA=public

##### RerankConfig
# Choices: [local]. The type of the reranker service.
RERANKER_SERVICE_TYPE=local
# Required str. The base URL of the reranker service.
RERANKER_BASE_URL=
# Required SecretStr. The API key of the reranker service.
RERANKER_API_KEY=
# Required str. The entry point of the reranker service.
RERANKER_ENTRY_POINT=/rerank
# Required str. The model name of the reranker service.
RERANKER_MODEL_NAME=Qwen3-Reranker-8B
# Required int. The max number of requests per unit time.
RERANKER_RATE_LIMIT=6000
# Choices: [second, minute, hour]. The time unit for the rate limit.
RERANKER_RATE_LIMIT_TIME_UNIT=minute
# Required float. The min interval in seconds between requests to the embedding service.
RERANKER_RATE_LIMIT_MIN_INTERVAL_SECONDS=0.1

##### TranslatorConfig
# Choices: [openai, local]. The type of the translator service.
TRANSLATOR_SERVICE_TYPE=local
# Required str. The base URL of the translator service. If service type is openai, it will be overwritten by openai_base_url.
TRANSLATOR_BASE_URL=
# Required SecretStr. The API key of the translator service. If service type is openai, it will be overwritten by openai_api_key.
TRANSLATOR_API_KEY=
# Required str. The entry point of the translator service.
TRANSLATOR_ENTRY_POINT=/v1/chat/completions
# Optional str. The model name of the translator service. Required when service type is local.
TRANSLATOR_MODEL_NAME=Hunyuan-MT-7B
# Optional float. The timeout in seconds for the translator requests.
TRANSLATOR_TIMEOUT=3600.0
# Optional int. The maximum number of tokens that can be generated in the chat completion.
TRANSLATOR_MAX_TOKENS=
# Optional float. 
TRANSLATOR_TEMPERATURE=
# Required int. The max number of requests per unit time.
TRANSLATOR_RATE_LIMIT=60
# Choices: [second, minute, hour]. The time unit for the rate limit.
TRANSLATOR_RATE_LIMIT_TIME_UNIT=minute
# Required float. The min interval in seconds between requests to the embedding service.
TRANSLATOR_RATE_LIMIT_MIN_INTERVAL_SECONDS=0.1
# Optional str. The base URL of the OpenAI embedding service. Useful only when service type is openai.
TRANSLATOR_OPENAI_BASE_URL=
# Optional SecretStr. The API key of the OpenAI embedding service. Useful only when service type is openai.
TRANSLATOR_OPENAI_API_KEY=
